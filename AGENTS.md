# 项目协作约定

## 语言规范

- 一般交流、说明、总结和项目文档正文默认使用中文。
- AI、Agent、LLM、Qt、QWidget、SurfaceSpec、LayoutPlan、API 等专有技术名词可以保留英文；代码标识符、协议字段、命令和文件路径保持原样。
- 当工具或文档格式依赖固定英文关键字时，保留该关键字，其余可读内容使用中文。

## 设计原则

- 项目设计以 MVP（最小可用产品）为优先，只实现当前业务目标所必需的能力。
- 避免与当前业务需求无关的冗余改动、预先抽象和复杂设计；不得为了假设性的未来场景扩大协议、组件或架构范围。
- 后续能力根据真实业务需求、验证结果和已发现问题渐进式推进；只有现有方案无法满足已确认需求时，才引入下一层设计。

## 文档组织

- 文档按“模块或主题—概念与职责—字段或机制—示例与边界”的逻辑顺序组织。
- 每段内容必须放在与其直接相关的模块或主题子标题下；不要把同一概念的定义、原理和限制散落在无关章节。
- 同一事实只在职责最匹配的文档或章节完整说明，其他位置保留必要摘要和链接，避免重复描述。
- 新增内容前先检查现有标题和段落；能够补入既有相关章节时，不新增平行章节或重复小节。
